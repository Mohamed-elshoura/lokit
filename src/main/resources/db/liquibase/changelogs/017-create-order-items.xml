<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="017-create-order-items" author="MohamedElshoura">
        <createTable tableName="order_items">

            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" primaryKeyName="pk_order_items" nullable="false"/>
            </column>

            <column name="order_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_order_items_orders" references="orders(id)"/>
            </column>

            <column name="variant_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_order_items_product_variants" references="product_variants(id)"/>
            </column>

            <column name="price" type="DECIMAL(12,2)">
                <constraints nullable="false"/>
            </column>

            <column name="quantity" type="INT">
                <constraints nullable="false" checkConstraint="quantity > 0"/>
            </column>

            <column name="line_total" type="DECIMAL(12,2)">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <createIndex tableName="order_items" indexName="idx_order_items_order">
            <column name="order_id"/>
        </createIndex>

        <createIndex tableName="order_items" indexName="idx_order_items_variant">
            <column name="variant_id"/>
        </createIndex>

        <rollback>
            <dropTable tableName="order_items"/>
        </rollback>
    </changeSet>
</databaseChangeLog>